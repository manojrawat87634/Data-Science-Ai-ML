mkdir -p numpy/VVIP_NumPy_Questions && cat << 'EOF' > numpy/VVIP_NumPy_Questions/README.md
# VVIP NumPy Questions

These questions focus on **real NumPy thinking** like reshaping, broadcasting, indexing, and vectorization.

---

## 1. Reverse Array
Reverse a NumPy array without using Python loops.

Example:
Input: [1,2,3,4,5]
Output: [5,4,3,2,1]

---

## 2. Find Non-Zero Elements
Given an array, return the indices of non-zero elements.

Example:
[1,0,2,0,3]

---

## 3. Create Chessboard Pattern
Create an 8x8 matrix like a chessboard using NumPy.

Example:
0 1 0 1
1 0 1 0

---

## 4. Normalize Array
Normalize values between 0 and 1.

Formula:
(x - min) / (max - min)

---

## 5. Find Unique Elements
Given an array with duplicates, return unique values.

Example:
[1,2,2,3,3,4]

---

## 6. Matrix Multiplication
Multiply two matrices using NumPy.

---

## 7. Find Row With Maximum Sum
Given a 2D array, find the row index with the highest sum.

---

## 8. Replace Max Value
Replace the maximum value in an array with 0.

---

## 9. Flatten and Reshape
Flatten a matrix and reshape it back to original dimensions.

---

## 10. Broadcasting Trick
Add a vector to every row of a matrix without loops.

---

## 11. Find Closest Value
Find the element in an array closest to a given number.

Example:
Array: [10,20,30,40]
Target: 26

Answer: 30

---

## 12. Diagonal Matrix
Create a diagonal matrix from a given array.

Example:
Input: [1,2,3]

Output:
1 0 0
0 2 0
0 0 3

---

## 13. Swap Two Rows
Swap two rows of a matrix using NumPy.

---

## 14. Random Sampling
Generate 10 random numbers between 1 and 100.

---

## 15. Detect Duplicate Values
Find duplicate elements in a NumPy array.

---

## Goal

These problems help master:

- Indexing
- Broadcasting
- Vectorization
- Matrix operations
- Real NumPy thinking

EOF